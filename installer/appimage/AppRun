#!/bin/bash
# AppImage entry point. The executable stands alone: the Linux build ships no
# speech library, so there is nothing beside it to find.
HERE="$(dirname "$(readlink -f "$0")")"
exec "$HERE/usr/bin/PortkeyDrop" "$@"
