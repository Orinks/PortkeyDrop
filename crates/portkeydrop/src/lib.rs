//! Portkey Drop's front end: the wxWidgets window and its supporting logic.
//!
//! The engine lives in `portkeydrop-core`; this crate is the part that draws
//! things, listens for keys, and speaks.

pub mod cli;
pub mod console;
pub mod single_instance;
pub mod ui;
